
## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Setup `.env.local` using `.env.example`
3. Set the API keys in `.env.local`.
4. Run the app:
   `npm run dev`


## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Setup `.env.local` using `.env.example`
2. Set the `GEMINI_API_KEY` or `OPEN_API_KEY` in `.env.local` to your API keys
3. Run the app:
   `npm run dev`
